version: 2

sources:
  - name: tpch_now
    database: raw  
    schema: tpch_now 
    tables:
      - name: lineitem
        config:
          freshness:
            warn_after:
              count: 6
              period: hour
            # error_after:
            #   count: 12
            #   period: hour
          loaded_at_field: "CAST (l_commitdate AS TIMESTAMP)"
      - name: orders
  - name: tpch_sf001
    database: raw
    schema: tpch_sf001
    tables:
      - name: partsupp
      - name: part
      - name: supplier
    
